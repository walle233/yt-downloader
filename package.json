{
  "name": "yt-video-downloader",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace @ytvd/web",
    "build:web": "npm run build --workspace @ytvd/web",
    "lint:web": "npm run lint --workspace @ytvd/web"
  }
}

